# Backend - Edu Tracker

Instalación:
1. cd backend
2. npm install
3. cp .env.example .env (editar si quieres)
4. npm run seed
5. npm run dev

Por defecto usa SQLite (database.sqlite en la raíz del backend). Para usar PostgreSQL setear DATABASE_URL en .env.
